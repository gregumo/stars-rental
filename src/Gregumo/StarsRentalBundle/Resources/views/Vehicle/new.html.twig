{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nouveau véhicule</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('vehicle') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
