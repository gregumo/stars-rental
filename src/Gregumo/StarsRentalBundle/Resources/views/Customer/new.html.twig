{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nouveau client</h1>

    {{ form(form, { 'label': 'Enregistrer' }) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('customer') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
